#include <stdio.h>
/**
 * main - This program will use printf to print with proper grammar,
 * but the outcome is a piece of art
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
