#include <stdio.h>
/**
 * main - prints alphabets in lowercase and then in uppercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	char o;
	char t;

	for (a = 'a'; y <= 'z'; y++)
	{
		putchar(y);
	}
	for (o = 'A'; o <= 'Z'; o++)
	{
		putchar(o);
	}
		putchar('\n');
	return (0);
}
